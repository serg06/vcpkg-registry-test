diff --git a/CMakeLists.txt b/CMakeLists.txt
index 120db2b..7b066e2 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -199,15 +199,67 @@ add_subdirectory(${CEF_LIBCEF_DLL_WRAPPER_PATH} libcef_dll_wrapper)
 # Include application targets.
 # Comes from the <target>/CMakeLists.txt file in the current directory.
 # TODO: Change these lines to match your project target when you copy this file.
-if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/tests")
-  add_subdirectory(tests/cefsimple)
-  add_subdirectory(tests/gtest)
-  add_subdirectory(tests/ceftests)
-endif()
-
-if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/tests/cefclient")
-  add_subdirectory(tests/cefclient)
-endif()
+
+# PATCH: COMMENTED OUT
+
+# if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/tests")
+#   add_subdirectory(tests/cefsimple)
+#   add_subdirectory(tests/gtest)
+#   add_subdirectory(tests/ceftests)
+# endif()
+#
+# if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/tests/cefclient")
+#   add_subdirectory(tests/cefclient)
+# endif()
+
+# INSTALL
+INSTALL(
+  TARGETS libcef_dll_wrapper
+  CONFIGURATIONS Debug
+  # DESTINATION debug/lib
+)
+
+# TODO: Try without fileglob (just use install pattern)?
+file(GLOB libs Debug/*.lib)
+list(FILTER libs EXCLUDE REGEX .*cef_sandbox.lib)  # Doesn't work on Windows
+INSTALL(
+  FILES ${libs}
+  CONFIGURATIONS Debug
+  TYPE LIB
+)
+
+# pdb
+INSTALL(
+  FILES
+    ${CMAKE_CURRENT_BINARY_DIR}/libcef_dll_wrapper/Debug/libcef_dll_wrapper.pdb
+  CONFIGURATIONS Debug
+  TYPE LIB
+)
+
+file(GLOB bins Debug/*.dll Debug/*.bin)
+INSTALL(
+  FILES ${bins}
+  CONFIGURATIONS Debug
+  TYPE BIN
+)
+
+file(GLOB bins Debug/swiftshader/*.dll)
+INSTALL(
+  FILES ${bins}
+  CONFIGURATIONS Debug
+  DESTINATION bin/swiftshader
+)
+
+INSTALL(
+  DIRECTORY include
+  DESTINATION .
+)
+
+# Fix for CEF imports
+INSTALL(
+  DIRECTORY include
+  DESTINATION include
+)
 
 # Display configuration settings.
 PRINT_CEF_CONFIG()
